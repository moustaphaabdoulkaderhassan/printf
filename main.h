#ifndef MAIN_H
#define MAIN_H

/* Add libraries */
#include <stdarg.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

/* Prototypes */
int printChar(int ch);
int printString(const char *string);
int _putchar(char c);
int _printf(const char *format, ...);

#endif /* MAIN_H */
